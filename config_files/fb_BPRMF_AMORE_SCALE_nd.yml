setting:
  data: facebook_books # Dataset
  gpu_id: 0 # GPU_ID
  baseline: BPRMF # Base model
  wrapper: AMORE_SCALE # Multi-Objective Method
  epochs: 500 # Number of epochs
  validation_rate: 10 # Period of evaluation during training
  validation_metric: ndcg@20 # Metric of evaluation during training
  batch_size: 2048 # Batch size
hyperparameters:
  dim: [ 64 ] # Dimension for embedding
  lr: [ 0.005, 0.001, 0.0005 ] # Learning rate
  l_2: [ 0.01, 0.005, 0.001 ] #L2 penalty
  mode: [ rnd ] # Which loss to use
  atk: [ { atk_div: 20, atk_nov: 20 } ] # Cutoff when computing approximated metrics
  ranker: [ AIMLE ]
  scale: [ 0.25, 0.5, 0.75, 0.95 ] # [0.25, 0.5, 0.75, 0.9]
  item_feature_path: [ 'arrays/facebook_books/BPRMF_None_1-backbone=BPRMF-mo_method=None-mode=r-data=facebook_books-seed=42-dim=64-lr=0$005-weight_decay=0$01-scale1=1.npz' ]




